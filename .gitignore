# Lots of junk goes in here.
/caches/
/backups/

# Top secret stuff!
/link/.ssh/*
!/link/.ssh/config

# Don't commit Vim plugins, vim-plug will install them.
/link/.vim/plugged

# Don't commit Atom packages
/link/.atom/**/*
!/link/.atom/*.cson
!/link/.atom/*.less
!/link/.atom/*.coffee
!/link/.atom/*.txt

# Don't commit karabiner logs
/link/.karabiner.d/log/*
!/link/.karabiner.d/log/.gitkeep

# Don't commit rbenv temp files.
/link/.rbenv/shims/
/link/.rbenv/versions/
/link/.rbenv/version

# Ignore any file with LOCAL in the name (good for per-machine vars/functions)
*LOCAL*

!reference/**/*


!/link/.vimrc.local
!/copy/.local_aliases.sh

.DS_Store
